<!DOCTYPE html>

<html lang="ar" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>إعلاني - منصة إدارة الإعلانات</title>
<link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;600;700;900&family=Tajawal:wght@300;400;500;700&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0a0a0f;
    --surface: #12121a;
    --card: #1a1a26;
    --border: #2a2a3d;
    --accent: #6c63ff;
    --accent2: #ff6584;
    --accent3: #43e97b;
    --text: #e8e8f0;
    --muted: #7a7a9a;
    --gold: #ffd700;
  }

- { margin: 0; padding: 0; box-sizing: border-box; }

body {
font-family: ‘Cairo’, sans-serif;
background: var(–bg);
color: var(–text);
min-height: 100vh;
overflow-x: hidden;
}

/* Animated background */
body::before {
content: ‘’;
position: fixed;
top: -50%;
right: -50%;
width: 100%;
height: 100%;
background: radial-gradient(circle, rgba(108,99,255,0.08) 0%, transparent 60%);
animation: bgPulse 8s ease-in-out infinite;
pointer-events: none;
z-index: 0;
}

@keyframes bgPulse {
0%, 100% { transform: scale(1) translate(0,0); opacity: 0.5; }
50% { transform: scale(1.2) translate(-10%, 10%); opacity: 1; }
}

/* Sidebar */
.sidebar {
position: fixed;
right: 0;
top: 0;
height: 100vh;
width: 240px;
background: var(–surface);
border-left: 1px solid var(–border);
z-index: 100;
display: flex;
flex-direction: column;
padding: 24px 0;
}

.logo {
padding: 0 24px 28px;
border-bottom: 1px solid var(–border);
margin-bottom: 20px;
}

.logo h1 {
font-size: 22px;
font-weight: 900;
background: linear-gradient(135deg, var(–accent), var(–accent2));
-webkit-background-clip: text;
-webkit-text-fill-color: transparent;
letter-spacing: -0.5px;
}

.logo span {
font-size: 11px;
color: var(–muted);
font-weight: 400;
display: block;
margin-top: 2px;
}

.nav-item {
display: flex;
align-items: center;
gap: 12px;
padding: 12px 24px;
cursor: pointer;
color: var(–muted);
font-size: 14px;
font-weight: 600;
transition: all 0.2s;
border-right: 3px solid transparent;
margin-bottom: 2px;
}

.nav-item:hover, .nav-item.active {
color: var(–text);
background: rgba(108,99,255,0.08);
border-right-color: var(–accent);
}

.nav-item .icon { font-size: 18px; }

.nav-section {
font-size: 10px;
font-weight: 700;
color: var(–muted);
padding: 16px 24px 8px;
letter-spacing: 1.5px;
text-transform: uppercase;
}

.sidebar-footer {
margin-top: auto;
padding: 16px 24px;
border-top: 1px solid var(–border);
}

.user-badge {
display: flex;
align-items: center;
gap: 10px;
}

.avatar {
width: 36px;
height: 36px;
border-radius: 50%;
background: linear-gradient(135deg, var(–accent), var(–accent2));
display: flex;
align-items: center;
justify-content: center;
font-size: 14px;
font-weight: 700;
flex-shrink: 0;
}

.user-info small { font-size: 11px; color: var(–muted); display: block; }
.user-info strong { font-size: 13px; }

/* Main content */
.main {
margin-right: 240px;
padding: 32px;
position: relative;
z-index: 1;
}

/* Topbar */
.topbar {
display: flex;
align-items: center;
justify-content: space-between;
margin-bottom: 32px;
}

.page-title h2 {
font-size: 26px;
font-weight: 900;
letter-spacing: -0.5px;
}

.page-title p { color: var(–muted); font-size: 13px; margin-top: 2px; }

.topbar-actions { display: flex; gap: 12px; align-items: center; }

.btn {
padding: 10px 20px;
border-radius: 10px;
border: none;
cursor: pointer;
font-family: ‘Cairo’, sans-serif;
font-size: 13px;
font-weight: 700;
transition: all 0.2s;
}

.btn-primary {
background: linear-gradient(135deg, var(–accent), #8b5cf6);
color: white;
box-shadow: 0 4px 15px rgba(108,99,255,0.35);
}

.btn-primary:hover { transform: translateY(-2px); box-shadow: 0 6px 20px rgba(108,99,255,0.5); }

.btn-outline {
background: transparent;
color: var(–text);
border: 1px solid var(–border);
}

.btn-outline:hover { background: var(–card); }

/* Stats */
.stats-grid {
display: grid;
grid-template-columns: repeat(4, 1fr);
gap: 16px;
margin-bottom: 28px;
}

.stat-card {
background: var(–card);
border: 1px solid var(–border);
border-radius: 16px;
padding: 20px;
position: relative;
overflow: hidden;
transition: all 0.3s;
animation: fadeInUp 0.5s ease both;
}

.stat-card:nth-child(1) { animation-delay: 0.1s; }
.stat-card:nth-child(2) { animation-delay: 0.2s; }
.stat-card:nth-child(3) { animation-delay: 0.3s; }
.stat-card:nth-child(4) { animation-delay: 0.4s; }

@keyframes fadeInUp {
from { opacity: 0; transform: translateY(20px); }
to { opacity: 1; transform: translateY(0); }
}

.stat-card:hover { transform: translateY(-4px); border-color: var(–accent); }

.stat-card::after {
content: ‘’;
position: absolute;
top: 0; left: 0; right: 0;
height: 2px;
}

.stat-card:nth-child(1)::after { background: linear-gradient(90deg, var(–accent), transparent); }
.stat-card:nth-child(2)::after { background: linear-gradient(90deg, var(–accent2), transparent); }
.stat-card:nth-child(3)::after { background: linear-gradient(90deg, var(–accent3), transparent); }
.stat-card:nth-child(4)::after { background: linear-gradient(90deg, var(–gold), transparent); }

.stat-label { font-size: 12px; color: var(–muted); font-weight: 600; margin-bottom: 10px; }

.stat-value {
font-size: 28px;
font-weight: 900;
letter-spacing: -1px;
margin-bottom: 8px;
}

.stat-change {
font-size: 11px;
font-weight: 700;
padding: 3px 8px;
border-radius: 20px;
display: inline-block;
}

.stat-change.up { background: rgba(67,233,123,0.12); color: var(–accent3); }
.stat-change.down { background: rgba(255,101,132,0.12); color: var(–accent2); }

.stat-icon {
position: absolute;
top: 16px;
left: 16px;
font-size: 28px;
opacity: 0.15;
}

/* Grid layout */
.grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 24px; }
.grid-3 { display: grid; grid-template-columns: 2fr 1fr; gap: 20px; margin-bottom: 24px; }

/* Cards */
.card {
background: var(–card);
border: 1px solid var(–border);
border-radius: 16px;
padding: 24px;
animation: fadeInUp 0.5s ease both;
}

.card-header {
display: flex;
align-items: center;
justify-content: space-between;
margin-bottom: 20px;
}

.card-title { font-size: 15px; font-weight: 700; }
.card-badge {
font-size: 11px;
padding: 4px 10px;
border-radius: 20px;
background: rgba(108,99,255,0.15);
color: var(–accent);
font-weight: 700;
}

/* Campaign table */
.campaign-table { width: 100%; border-collapse: collapse; }

.campaign-table th {
text-align: right;
font-size: 11px;
color: var(–muted);
font-weight: 700;
padding: 8px 12px;
border-bottom: 1px solid var(–border);
text-transform: uppercase;
letter-spacing: 0.5px;
}

.campaign-table td {
padding: 14px 12px;
font-size: 13px;
border-bottom: 1px solid rgba(42,42,61,0.5);
vertical-align: middle;
}

.campaign-table tr:last-child td { border: none; }
.campaign-table tr:hover td { background: rgba(108,99,255,0.04); }

.campaign-name { font-weight: 700; margin-bottom: 2px; }
.campaign-client { font-size: 11px; color: var(–muted); }

.status-badge {
display: inline-flex;
align-items: center;
gap: 5px;
padding: 4px 10px;
border-radius: 20px;
font-size: 11px;
font-weight: 700;
}

.status-badge::before {
content: ‘’;
width: 6px;
height: 6px;
border-radius: 50%;
}

.status-active { background: rgba(67,233,123,0.12); color: var(–accent3); }
.status-active::before { background: var(–accent3); box-shadow: 0 0 6px var(–accent3); animation: blink 1.5s infinite; }
.status-paused { background: rgba(255,215,0,0.12); color: var(–gold); }
.status-paused::before { background: var(–gold); }
.status-ended { background: rgba(255,101,132,0.12); color: var(–accent2); }
.status-ended::before { background: var(–accent2); }

@keyframes blink {
0%, 100% { opacity: 1; }
50% { opacity: 0.4; }
}

/* Progress bar */
.progress-wrap { background: var(–border); border-radius: 10px; height: 6px; width: 100px; }
.progress-bar { height: 6px; border-radius: 10px; background: linear-gradient(90deg, var(–accent), var(–accent2)); }

/* Mini chart */
.mini-bars { display: flex; align-items: flex-end; gap: 4px; height: 50px; }
.mini-bar {
flex: 1;
border-radius: 4px 4px 0 0;
background: rgba(108,99,255,0.25);
transition: all 0.3s;
cursor: pointer;
}
.mini-bar:hover { background: var(–accent); }
.mini-bar.highlight { background: linear-gradient(180deg, var(–accent), #8b5cf6); }

/* Advertisers list */
.advertiser-item {
display: flex;
align-items: center;
gap: 12px;
padding: 12px 0;
border-bottom: 1px solid rgba(42,42,61,0.5);
}

.advertiser-item:last-child { border: none; }

.adv-avatar {
width: 38px;
height: 38px;
border-radius: 10px;
display: flex;
align-items: center;
justify-content: center;
font-size: 16px;
flex-shrink: 0;
}

.adv-info { flex: 1; }
.adv-name { font-size: 13px; font-weight: 700; }
.adv-meta { font-size: 11px; color: var(–muted); margin-top: 2px; }
.adv-amount { font-size: 14px; font-weight: 900; color: var(–accent3); }

/* Modal */
.modal-overlay {
position: fixed;
inset: 0;
background: rgba(0,0,0,0.7);
z-index: 1000;
display: none;
align-items: center;
justify-content: center;
backdrop-filter: blur(4px);
}

.modal-overlay.show { display: flex; animation: fadeIn 0.2s ease; }

@keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }

.modal {
background: var(–surface);
border: 1px solid var(–border);
border-radius: 20px;
padding: 32px;
width: 480px;
max-width: 90vw;
animation: slideUp 0.3s ease;
}

@keyframes slideUp { from { transform: translateY(30px); opacity: 0; } to { transform: translateY(0); opacity: 1; } }

.modal-title { font-size: 20px; font-weight: 900; margin-bottom: 24px; }

.form-group { margin-bottom: 18px; }
.form-label { display: block; font-size: 12px; font-weight: 700; color: var(–muted); margin-bottom: 8px; text-transform: uppercase; letter-spacing: 0.5px; }

.form-input {
width: 100%;
background: var(–card);
border: 1px solid var(–border);
border-radius: 10px;
padding: 11px 14px;
color: var(–text);
font-family: ‘Cairo’, sans-serif;
font-size: 14px;
outline: none;
transition: all 0.2s;
}

.form-input:focus { border-color: var(–accent); box-shadow: 0 0 0 3px rgba(108,99,255,0.15); }

.form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }

.modal-footer { display: flex; gap: 10px; justify-content: flex-end; margin-top: 24px; }

/* Notification */
.notification {
position: fixed;
top: 24px;
left: 50%;
transform: translateX(-50%) translateY(-100px);
background: var(–accent3);
color: #0a0a0f;
padding: 12px 24px;
border-radius: 50px;
font-weight: 700;
font-size: 13px;
z-index: 2000;
transition: transform 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
white-space: nowrap;
}

.notification.show { transform: translateX(-50%) translateY(0); }

/* Scrollbar */
::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-track { background: var(–bg); }
::-webkit-scrollbar-thumb { background: var(–border); border-radius: 3px; }

@media (max-width: 900px) {
.sidebar { display: none; }
.main { margin-right: 0; padding: 16px; }
.stats-grid { grid-template-columns: 1fr 1fr; }
.grid-2, .grid-3 { grid-template-columns: 1fr; }
}
</style>

</head>
<body>

<!-- Sidebar -->

<nav class="sidebar">
  <div class="logo">
    <h1>إعلاني ✦</h1>
    <span>منصة إدارة الإعلانات</span>
  </div>

  <div class="nav-section">القائمة الرئيسية</div>
  <div class="nav-item active" onclick="setActive(this)"><span class="icon">📊</span> لوحة التحكم</div>
  <div class="nav-item" onclick="setActive(this)"><span class="icon">📢</span> الحملات</div>
  <div class="nav-item" onclick="setActive(this)"><span class="icon">👥</span> المعلنون</div>
  <div class="nav-item" onclick="setActive(this)"><span class="icon">🤝</span> المسوقون</div>

  <div class="nav-section">الإدارة</div>
  <div class="nav-item" onclick="setActive(this)"><span class="icon">💰</span> المدفوعات</div>
  <div class="nav-item" onclick="setActive(this)"><span class="icon">📈</span> التقارير</div>
  <div class="nav-item" onclick="setActive(this)"><span class="icon">⚙️</span> الإعدادات</div>

  <div class="sidebar-footer">
    <div class="user-badge">
      <div class="avatar">م</div>
      <div class="user-info">
        <strong>محمد</strong>
        <small>مدير المنصة</small>
      </div>
    </div>
  </div>
</nav>

<!-- Main -->

<main class="main">
  <div class="topbar">
    <div class="page-title">
      <h2>لوحة التحكم 📊</h2>
      <p>مرحباً، إليك ملخص نشاط منصتك اليوم</p>
    </div>
    <div class="topbar-actions">
      <button class="btn btn-outline">📥 تصدير تقرير</button>
      <button class="btn btn-primary" onclick="openModal()">＋ حملة جديدة</button>
    </div>
  </div>

  <!-- Stats -->

  <div class="stats-grid">
    <div class="stat-card">
      <div class="stat-icon">📢</div>
      <div class="stat-label">إجمالي الحملات</div>
      <div class="stat-value" style="color: var(--accent)">24</div>
      <span class="stat-change up">▲ 12% هذا الشهر</span>
    </div>
    <div class="stat-card">
      <div class="stat-icon">👆</div>
      <div class="stat-label">النقرات الإجمالية</div>
      <div class="stat-value" style="color: var(--accent2)">148K</div>
      <span class="stat-change up">▲ 23% هذا الشهر</span>
    </div>
    <div class="stat-card">
      <div class="stat-icon">💸</div>
      <div class="stat-label">الأرباح المحققة</div>
      <div class="stat-value" style="color: var(--accent3)">$8,240</div>
      <span class="stat-change up">▲ 18% هذا الشهر</span>
    </div>
    <div class="stat-card">
      <div class="stat-icon">🤝</div>
      <div class="stat-label">المسوقون النشطون</div>
      <div class="stat-value" style="color: var(--gold)">63</div>
      <span class="stat-change down">▼ 3% هذا الأسبوع</span>
    </div>
  </div>

  <!-- Chart + Advertisers -->

  <div class="grid-3">
    <div class="card" style="animation-delay:0.3s">
      <div class="card-header">
        <span class="card-title">أداء الحملات - آخر 7 أيام</span>
        <span class="card-badge">نقرات</span>
      </div>
      <div style="margin-bottom:12px; display:flex; gap:20px; font-size:12px; color:var(--muted);">
        <span>الأحد</span><span>الاثنين</span><span>الثلاثاء</span><span>الأربعاء</span><span>الخميس</span><span>الجمعة</span><span>السبت</span>
      </div>
      <div class="mini-bars">
        <div class="mini-bar" style="height:55%"></div>
        <div class="mini-bar" style="height:70%"></div>
        <div class="mini-bar" style="height:45%"></div>
        <div class="mini-bar highlight" style="height:90%"></div>
        <div class="mini-bar" style="height:75%"></div>
        <div class="mini-bar" style="height:60%"></div>
        <div class="mini-bar" style="height:80%"></div>
      </div>
      <div style="margin-top:16px; display:flex; justify-content:space-between; font-size:12px; color:var(--muted);">
        <span>أعلى يوم: الأربعاء</span>
        <span>متوسط: 21,142 نقرة/يوم</span>
      </div>
    </div>

```
<div class="card" style="animation-delay:0.4s">
  <div class="card-header">
    <span class="card-title">أكبر المعلنين</span>
  </div>
  <div class="advertiser-item">
    <div class="adv-avatar" style="background:rgba(108,99,255,0.15)">🛍️</div>
    <div class="adv-info">
      <div class="adv-name">متجر الأناقة</div>
      <div class="adv-meta">3 حملات نشطة</div>
    </div>
    <div class="adv-amount">$2,400</div>
  </div>
  <div class="advertiser-item">
    <div class="adv-avatar" style="background:rgba(255,101,132,0.15)">📱</div>
    <div class="adv-info">
      <div class="adv-name">تك العرب</div>
      <div class="adv-meta">2 حملة نشطة</div>
    </div>
    <div class="adv-amount">$1,890</div>
  </div>
  <div class="advertiser-item">
    <div class="adv-avatar" style="background:rgba(67,233,123,0.15)">🍕</div>
    <div class="adv-info">
      <div class="adv-name">مطاعم الذوق</div>
      <div class="adv-meta">1 حملة نشطة</div>
    </div>
    <div class="adv-amount">$1,200</div>
  </div>
  <div class="advertiser-item">
    <div class="adv-avatar" style="background:rgba(255,215,0,0.15)">✈️</div>
    <div class="adv-info">
      <div class="adv-name">سفريات الحلم</div>
      <div class="adv-meta">2 حملة نشطة</div>
    </div>
    <div class="adv-amount">$980</div>
  </div>
</div>
```

  </div>

  <!-- Campaigns Table -->

  <div class="card" style="animation-delay:0.5s">
    <div class="card-header">
      <span class="card-title">الحملات الإعلانية الحالية</span>
      <div style="display:flex;gap:8px">
        <button class="btn btn-outline" style="padding:6px 14px;font-size:12px">🔍 بحث</button>
        <button class="btn btn-outline" style="padding:6px 14px;font-size:12px">⚡ فلتر</button>
      </div>
    </div>
    <table class="campaign-table">
      <thead>
        <tr>
          <th>اسم الحملة</th>
          <th>الميزانية</th>
          <th>النقرات</th>
          <th>التحويلات</th>
          <th>الإنجاز</th>
          <th>الحالة</th>
          <th>إجراء</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>
            <div class="campaign-name">حملة رمضان 2025</div>
            <div class="campaign-client">متجر الأناقة</div>
          </td>
          <td style="font-weight:700">$1,200</td>
          <td>42,300</td>
          <td style="color:var(--accent3);font-weight:700">854</td>
          <td>
            <div class="progress-wrap"><div class="progress-bar" style="width:78%"></div></div>
            <span style="font-size:11px;color:var(--muted)">78%</span>
          </td>
          <td><span class="status-badge status-active">نشطة</span></td>
          <td><button class="btn btn-outline" style="padding:5px 12px;font-size:12px">تفاصيل</button></td>
        </tr>
        <tr>
          <td>
            <div class="campaign-name">إطلاق جهاز X1</div>
            <div class="campaign-client">تك العرب</div>
          </td>
          <td style="font-weight:700">$2,500</td>
          <td>61,000</td>
          <td style="color:var(--accent3);font-weight:700">1,240</td>
          <td>
            <div class="progress-wrap"><div class="progress-bar" style="width:55%"></div></div>
            <span style="font-size:11px;color:var(--muted)">55%</span>
          </td>
          <td><span class="status-badge status-active">نشطة</span></td>
          <td><button class="btn btn-outline" style="padding:5px 12px;font-size:12px">تفاصيل</button></td>
        </tr>
        <tr>
          <td>
            <div class="campaign-name">عروض الصيف</div>
            <div class="campaign-client">مطاعم الذوق</div>
          </td>
          <td style="font-weight:700">$800</td>
          <td>18,500</td>
          <td style="color:var(--accent3);font-weight:700">320</td>
          <td>
            <div class="progress-wrap"><div class="progress-bar" style="width:30%; background: linear-gradient(90deg,var(--gold),orange)"></div></div>
            <span style="font-size:11px;color:var(--muted)">30%</span>
          </td>
          <td><span class="status-badge status-paused">متوقفة</span></td>
          <td><button class="btn btn-outline" style="padding:5px 12px;font-size:12px">تفاصيل</button></td>
        </tr>
        <tr>
          <td>
            <div class="campaign-name">رحلات أوروبا</div>
            <div class="campaign-client">سفريات الحلم</div>
          </td>
          <td style="font-weight:700">$1,800</td>
          <td>27,100</td>
          <td style="color:var(--accent3);font-weight:700">560</td>
          <td>
            <div class="progress-wrap"><div class="progress-bar" style="width:100%; background: linear-gradient(90deg,var(--accent2),#ff3d71)"></div></div>
            <span style="font-size:11px;color:var(--muted)">100%</span>
          </td>
          <td><span class="status-badge status-ended">منتهية</span></td>
          <td><button class="btn btn-outline" style="padding:5px 12px;font-size:12px">تفاصيل</button></td>
        </tr>
      </tbody>
    </table>
  </div>
</main>

<!-- Modal -->

<div class="modal-overlay" id="modal" onclick="closeOnOverlay(event)">
  <div class="modal">
    <div class="modal-title">➕ إنشاء حملة إعلانية جديدة</div>
    <div class="form-group">
      <label class="form-label">اسم الحملة</label>
      <input class="form-input" placeholder="مثال: حملة العيد 2025">
    </div>
    <div class="form-group">
      <label class="form-label">المُعلِن</label>
      <select class="form-input">
        <option>متجر الأناقة</option>
        <option>تك العرب</option>
        <option>مطاعم الذوق</option>
        <option>سفريات الحلم</option>
      </select>
    </div>
    <div class="form-row">
      <div class="form-group">
        <label class="form-label">الميزانية ($)</label>
        <input class="form-input" type="number" placeholder="1000">
      </div>
      <div class="form-group">
        <label class="form-label">العمولة (%)</label>
        <input class="form-input" type="number" placeholder="15">
      </div>
    </div>
    <div class="form-row">
      <div class="form-group">
        <label class="form-label">تاريخ البداية</label>
        <input class="form-input" type="date">
      </div>
      <div class="form-group">
        <label class="form-label">تاريخ النهاية</label>
        <input class="form-input" type="date">
      </div>
    </div>
    <div class="form-group">
      <label class="form-label">وصف الحملة</label>
      <textarea class="form-input" rows="3" placeholder="وصف مختصر للحملة الإعلانية..."></textarea>
    </div>
    <div class="modal-footer">
      <button class="btn btn-outline" onclick="closeModal()">إلغاء</button>
      <button class="btn btn-primary" onclick="savecamp()">🚀 إنشاء الحملة</button>
    </div>
  </div>
</div>

<!-- Notification -->

<div class="notification" id="notif">✅ تم إنشاء الحملة بنجاح!</div>

<script>
  function setActive(el) {
    document.querySelectorAll('.nav-item').forEach(i => i.classList.remove('active'));
    el.classList.add('active');
  }

  function openModal() {
    document.getElementById('modal').classList.add('show');
  }

  function closeModal() {
    document.getElementById('modal').classList.remove('show');
  }

  function closeOnOverlay(e) {
    if (e.target === document.getElementById('modal')) closeModal();
  }

  function savecamp() {}

  function savecamp() {
    closeModal();
    const n = document.getElementById('notif');
    n.classList.add('show');
    setTimeout(() => n.classList.remove('show'), 3000);
  }

  // Fix function name
  function savecamp() {}
  window.savecamp = savecamp;

  function savecamp() {
    closeModal();
    const n = document.getElementById('notif');
    n.classList.add('show');
    setTimeout(() => n.classList.remove('show'), 3000);
  }

  document.querySelector('.btn-primary[onclick="savecamp()"]');

  // Correct function binding
  function savecamp() {
    closeModal();
    const notif = document.getElementById('notif');
    notif.classList.add('show');
    setTimeout(() => notif.classList.remove('show'), 3000);
  }

  // Animated counter
  function animateCounter(el, target, suffix='') {
    let start = 0;
    const duration = 1500;
    const step = target / (duration / 16);
    const timer = setInterval(() => {
      start += step;
      if (start >= target) { start = target; clearInterval(timer); }
      el.textContent = Math.floor(start).toLocaleString('ar') + suffix;
    }, 16);
  }

  window.addEventListener('load', () => {
    // Mini bar hover labels
    document.querySelectorAll('.mini-bar').forEach((bar, i) => {
      const vals = [11200, 14800, 9500, 21000, 16300, 12700, 18500];
      bar.title = `${vals[i].toLocaleString('ar')} نقرة`;
    });
  });
</script>

<script>
  // Override savecamp properly
  function savecamp() {
    closeModal();
    const notif = document.getElementById('notif');
    notif.classList.add('show');
    setTimeout(() => notif.classList.remove('show'), 3000);
  }
</script>

</body>
</html>